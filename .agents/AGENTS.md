# Workspace Guidelines & Rules

## Git Push Policy (IMPORTANT)
- **NEVER** run `git push` or push code to remote repositories proactively or automatically.
- Always commit changes locally (`git commit`) when requested.
- Only run `git push` if the user explicitly asks for a push in their request.
